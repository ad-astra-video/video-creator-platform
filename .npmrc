# pnpm 11 runs a dependency re-install check before every `pnpm run <script>`.
# The ignored-builds warning makes that check exit non-zero; skip it so scripts
# (typecheck, dev, deploy) run directly. Builds are still installed on `pnpm install`.
verify-deps-before-run=false
